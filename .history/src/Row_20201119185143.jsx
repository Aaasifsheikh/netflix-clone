import React from "react";

function Row({ title }) {
   

    // it run snippet of code on the basis of specific condition
  useEffect(() => {
      effect
      return () => {
          cleanup
      }
  }, [input])

  return (
    <div className="">
      <h2>{title}</h2>
    </div>
  );
}

export default Row;
